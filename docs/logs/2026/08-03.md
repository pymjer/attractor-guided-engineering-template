# Development Log - 2026-08-03

### 2026-08-03 (pi driver 收尾 — 环境变量曝光 + pi skill 桥接)

- **pi driver 默认 model 问题（撤销 driver-aware 改动，改用环境变量）**: 用户报 `--driver pi` 时 model 报错（`missions/base.json` 的 `zhipuai-coding-plan/glm-5.2` 是 opencode 格式，pi 不认）。曾尝试把 `config.js` 的 model fallback 改 driver-aware（pi→`zai-coding-cn/glm-5.2`），但用户决定**不改默认值、用环境变量**——`MISSION_DRIVER_EXEC=pi OPENCODE_MODEL=zai-coding-cn/glm-5.2 ./tools/mission-driver.sh run onboarding`。config.js 的 model 解析链 `args.model → OPENCODE_MODEL → mission.model → 默认` 早已支持，无需改代码。
  - 过程教训: 撤销时误用 `git checkout -- config.js` 丢了 pi 执行期未提交代码（HEAD/gitee 无 PI_DEFAULTS，那些是工作区未提交改动）。后从 `git fsck` 发现的 dangling stash commit `c5b6273`（"On main: closure-audit-baseline"）完整恢复（config.js 759 行，PI_DEFAULTS/resolveDriverFields 齐全，pickDriverModel=0 确认无残留）。教训：对含大量未提交改动的工作区，撤销局部改动用 `edit` 反向操作，勿对整文件 `git checkout`。
- **`./tools/mission-driver.sh --help` 补环境变量 + 修执行权限**:
  - `src/main.js` help 段（addHelpText `after`）顶部加 `MISSION_DRIVER_EXEC` / `MISSION_DRIVER_ARGS` / `MISSION_PROMPT_MODE` 三行——代码早已读这三个 env（config.js 三返回点），只是 `--help` 没曝光，导致用户不知道能用环境变量切 driver。
  - `tools/mission-driver.sh` 模式 `100644 → 100755`（`chmod +x` + `git update-index --chmod=+x`）——之前 `./` 形式 Permission denied（根本看不到 help），clone/install 出去也缺 x。
- **pi skill 桥接（让 pi 也加载 mission-driver skill）**:
  - 背景: `.opencode/skills/mission-driver/`（SKILL.md + references/，git 跟踪 + `install-age.manifest` 第 38-41 行安装）opencode 自动加载；pi 读 `.pi/` 加载不到。pi 与 opencode 同为 [Agent Skills standard](https://agentskills.io/specification)，SKILL.md 内容完全兼容（frontmatter `name: mission-driver` + 详细 description）。
  - 方案演进（两次切换）:
    1. 初版 `.pi/settings.json` 的 `skills` 数组指向 `.opencode/skills`（pi 官方"Using Skills from Other Harnesses"机制）——**用户实测 pi 加载不到，废弃**。
    2. **最终方案（当前）**: 直接复制 skill 到 `.pi/skills/mission-driver/`（pi 原生扫描 `.pi/skills/` 找 SKILL.md，无需 settings.json 桥接）。代价是两份相同 SKILL.md（.opencode + .pi），但这是 pi 唯一可靠加载的方式。
  - `install-age.manifest`: 加 3 条 `pi-only` flag 条目（`.opencode/skills/mission-driver/X > .pi/skills/mission-driver/X :: pi-only`），复用现有复制循环，数据驱动（skill 加文件只改 manifest 一处）。
  - `tools/install-age.mjs`: 复制循环识别 `pi-only` flag——`if (entry.flags.includes("pi-only") && !withPi) continue;`（默认 skip，`--pi` 时复制）；删除 `ensurePiSkillsSetting` helper（settings.json 方案废弃）；删 `[6/7]` 步骤（.pi 复制并入 `[1/6]`），编号回 /6；Report/NEXT-STEPS 的 pi 行条件化（`withPi`）。`--pi` opt-in 保持（用户要求默认 install 不碰 .pi/）。`main()` 扫描 argv 分离 positional/flags，`installAge({ target, projectName, withPi })`。
  - dogfood 仓库 + `/Users/wuyz/proj/storage-treasure`: 删无效的 `.pi/settings.json`；复制 `.opencode/skills/mission-driver/` → `.pi/skills/mission-driver/`（SKILL.md + references/）。
- Verification: 端到端 `node tools/install-age.mjs <tmp>`——默认只复制 `.opencode/skills`（pi-only skip 生效）、`--pi` 额外复制 `.pi/skills/mission-driver/`（3 文件，输出标注 `(pi-only)`）、`.pi` 与 `.opencode` 的 SKILL.md `diff` 完全一致。`node --check tools/install-age.mjs` 通过。
- 残留: pi 项目级 skill 需用户在 pi 里 trust 项目后才加载（pi 安全策略）；`.pi/skills` 是 `.opencode/skills` 的副本，SKILL.md 改动需同步——install 时由 manifest 驱动，已 install 的项目需重跑 `--pi` 或手动同步。
