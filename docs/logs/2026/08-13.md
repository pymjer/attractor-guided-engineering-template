# Development Log - 2026-08-13

### 2026-08-13 (prompt-contract-optimization: 12-prompt 最小执行契约重写 + flow/loader/linter 契约修复)

- 执行 plan `docs/plans/mission-driver-prompt-contract-optimization/2026-08-12-1615-1-prompt-contract-optimization.md`（4 Phase，architecture change + implementation），全部 Phase Exit Criteria + Closure Gates 达成,Plan Status → `completed`。消除需求文档 `docs/requirements/mission-driver-prompt-contract-optimization.md` 列出的 E1–E14 全部契约缺陷。
- 背景: 内置 12 prompt 存在失效模板变量(`{{backlogDir}}` 未注入)、marker 顺序矛盾、不可达 closure 分支、clean audit 仍标 `open`、项目专属泄漏(Maven `-pl -am`/Jira/固定 commit 模板/字面量 `{timestamp}`)、EXECUTE 提前翻转 mission 级状态、BUILD_VERIFY 改码后不再 closure;`prompt-check.mjs` 远弱于真实契约;失败路径可被伪装成 `nothing/clean/completed`。E1–E14 均经本会话逐条读码独立核验属实。

- **两轮独立草案复审(subagent, fresh session)**: iter1 = NEEDS_REVISION(阻塞项 D4:draft prompt 内的"independent sub-agent"只是同一 agent step 内的自我编排,engine 无 session 隔离保障,可能不满足 plan-guide 规则 13,且把 E5 绕过路径正式化;另发现 6D CLOSURE_AUDIT.onMaxRetries 违反失败诚实、E9 覆盖不足)。人工裁决 **D4 = 选项 B**(保留 REVIEW_PLANS 为规范独立复审门,draft prompt 降为自查/预检不自升 active)+ 新增 **D7**(roadmap item ↔ plan 1:1)。iter2 = ACCEPT(确认选项 B 零 flow JSON 结构改动,与现有 DRAFT_PLANS→REVIEW_PLANS→EXEC_PLANS 流一致)。

- **Phase 1 — flow/loader 确定性修复**(Fix):
  - `flow-loader.js::closureScriptCheck` catch 分支新增 `flowVars.set("SCRIPT_CHECK_RESULT","FAIL")` + DETAILS,消除异常时 `{{SCRIPT_CHECK_RESULT}}` 残留。
  - `plan-execution.json` `CLOSURE_AUDIT.onMaxRetries` 由 `goto:BUILD_VERIFY` 改为 `done:"failed"`(6D:审计不收敛不再被 BUILD_VERIFY 掩盖)。
  - `mission-driver.json` `DRAFT_PLANS` 加 `fail:{done:"failed"}`;`deep-audit-loop.json` 的 `CHECK_OPEN_AUDITS`/`MULTI_AUDIT`/`OPEN_AUDIT`/`SCAN_NEW_RESULTS` 各加 `fail:{done:"failed"}`(承接 P3 诚实终止)。仅新增 transition 边,不新增 step/标签类型。
  - 新增 `test/closure-script-check.test.js`(2 断言)。
- **Phase 2 — 12 prompt 重写为最小执行契约**(Fix-heavy):`draft-from-roadmap`(选项B自查/D7 1:1/E5/E10/fail)、`draft-from-audit`(E1 `{{backlogDir}}`→`{{roadmapPath}}`/E10/fail/自查)、`closure-audit`(E2 删不可达 SCRIPT-PASS 分支/E3 消除 ONLY-marker↔REMAINING 矛盾/E4 对齐 `strict:false` 预检/D2 approved 仅证元数据漂移)、`execute`(E14 移除提前 roadmap-done/audit-close/closure 宣称 + 不设 Plan Status completed;D3 记录 dirty-path baseline)、`build-verify`(E6/E7 删 Maven/Jira/固定模板/`{timestamp}`、用 `{{commitFormat}}`、承担语义 closure + mission 级状态翻转 + dirty-path 安全提交、发现代码问题重开 phase 后 `fail`→EXECUTE)、`multi/open-audit`(E8 三态头部 clean/triaged/open + fail + 有界抽样)、`mission-draft`(E12 删装饰 `AI_STEP_RESULT`)、`run-postmortem`(E13 删装饰 marker + 放宽 What worked)、`plan-review`(选项B复审门 + D7 1:1 校验,`> Work Item:` 存在=roadmap 来源做 1:1,缺=audit 来源豁免)。`mission-brief`/`health-check` 保持 align-only(`{{backlogDir}}` 经核实在 draft/brief 命令路径 `main.js:403/467` 已注入,非 bug)。
- **Phase 3 — `prompt-check.mjs` 扩展**(Fix|Add):新增 `FLOW_INJECTED_VARS` 白名单(不含 backlogDir)、`LEAK_PATTERNS` 项目泄漏扫描;`lintPrompt` 增 `created⇒<FLOW_VARS>/<PLAN_FILE>` 且顺序在 marker 前(E10)、`issues⇒<REMAINING>`、forEach 步骤值校验(D1,`approved` 经 alias 合法);`lintFileLevel` 增 flow-bound marker 存在性 + 模板变量白名单 + 全 prompt 泄漏扫描(file-level 检查置于 `lintAllPrompts` 以不破坏 malformed-tag 单测的"恰好 1 错误"断言)。更新 `prompt-markers.test.js` forEach 断言(D1 反转);新增 `test/prompt-check-extended.test.js`。
- **Phase 4 — 四层静态与模拟回归**(Proof):新增 `test/prompt-contract-flow-sim.test.js`(7 断言:closure-pass 跳过 CLOSURE_AUDIT、onMaxRetries→failed、Option B DRAFT→REVIEW_PLANS、DRAFT_PLANS/deep-audit fail 边、DRAFT_PLANS `fail` engine-sim 终止 `failed` 不被洗成 nothing/completed、`_scanOpenAuditsList` 仅计 `open`)。纯模型语义场景以契约 marker + flow 结构 pin 静态代理,不做真实模型长跑(Non-Goal)。调用数:选项 B happy-path = DRAFT 1 + REVIEW_PLANS 1 + EXECUTE 1 + BUILD_VERIFY 1,与需求 P5 口径一致。

- Owner-doc: `tools/mission-driver/design/mission-driver-flow-design.md`(新增 2026-08-13 Changes 条目;修正 Design Conclusion #5、DRAFT_PLANS/REVIEW_PLANS 步骤描述为选项 B)、`docs/architecture/mission-driver-baseline.md`(Marker Contracts 段扩展:`AI_STEP_RESULT` 值集 + `fail` 诚实终止 + 扩展 linter 能力 + audit 三态头部 + 状态翻转所有权)已同步。`CONTEXT.md` flow-shape 描述未变且无矛盾陈述,无需 edit。需求文档已加 2026-08-12 决议更新块消除 P2"REVIEW_PLANS 零调用"漂移。
- Safety scope: 未触碰 engine.js 核心 `_result`/`_wfClose`/`_executeSubflowStep`、零 npm 依赖不变、`web/dist/` 未动、`memory/_index.md` 未动、`install-age.sh` 未动(均为 project-context.md AI Block Conditions)。仅新增 transition 边 + prompt 措辞 + linter 契约,未改状态机形状(`CHECK → REVIEW → EXECUTE → DRAFT → DEEP_AUDIT` 保留)。
- Verification (**full green**): `pnpm --prefix tools/mission-driver test` → **615 pass / 0 fail** + `prompt-check: OK`;`git diff --check` 清洁(修复 `health-check.md` EOF 空行);静态 grep 确认 flow prompt 无 `{{backlogDir}}`/Maven/Jira/`{timestamp}`/不可达分支残留。
- Closure: 独立收尾审计(subagent, fresh session)见 plan 的 Closure 段。
