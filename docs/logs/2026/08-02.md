# Development Log - 2026-08-02

### 2026-08-02 (pi driver support — mission-driver 可选 pi 执行器)

- 执行 plan `docs/plans/2026-08-02-2245-mission-driver-pi-driver-support-plan.md`（3 Phase），全部 Exit Criteria + Closure Gates 达成，Plan Status → `active`→待 closure audit。给 mission-driver 增加 `pi` 作为可选 driver，默认仍 `opencode`，`--driver pi` 一行切换。
- 背景: driver 抽象基础（`config.js` driver/driverArgs/promptMode + `runner.js` buildDriverArgs 模板替换）早已存在，但 `--driver` CLI 未暴露、无 pi 感知默认值、无 pi persona。用户要"很方便切换"+不改 spawn 架构（system-prompt 方式，非 subagent 扩展 RPC/in-process）。
- 改动（3 个非保护区 src 文件 + 1 新测试 + 1 新 persona + 4 owner docs，零依赖、零 engine.js、零 web/dist、零 mission.json schema）:
  - `src/config.js` — 新增 `PI_DEFAULTS`（driverArgs/promptMode/agentFile）+ `TOOL_ROOT`（`dirname(fileURLToPath(import.meta.url))`，对齐 flow-loader.js 模式）+ `resolveDriverFields()` helper。三个 resolveConfig 返回点（主/draft/analyze）应用 pi 默认值作为最低优先级回退，promptMode **永远输出具体值**（pi→`stdin`，其余→`arg`，绝不 undefined，防 `runner.js:27` `|| "stdin"` 兜底让 opencode 漂移）。`agentFile` 为计算字段（driver==pi 时解析为 `<TOOL_ROOT>/agents/build.pi.md` 绝对路径），非 mission.json 字段、不进 validateMission。**Fix（draft review M2）**: 主分支 `const driver = ... || "opencode"` 改 `|| undefined`，让 `mission.driver` 不再是死代码（与 driverArgs/promptMode 的 mission 字段优先级一致）。
  - `src/runner.js` — `buildDriverArgs()` 新增 `{agentFile}` 占位符替换（driver!=pi 时整体剔除 `@{agentFile}` 含前导 `@`，无残留 standalone arg）；`--pure`/`--variant` 仅 `config.driver !== "pi"` 时注入；`findLatestSessionId(projectRoot, driver)` driver==pi 时直接返回 null（跳过 `opencode session list`，防污染 run-state.json）。导出 `findLatestSessionId` 做测试缝（对齐 executor.js 导出 buildErrorTail 等的模式）。
  - `src/main.js` — `run` 子命令 + 主命令均注册 `.option("--driver <exe>", ...)`；`cmdRunMission` args 加 `driver: opts.driver`。
  - `test/pi-driver-config.test.js`（新，10 用例）— resolveConfig pi 默认值/显式覆盖/opencode 回归覆盖**全部三个返回点**；buildDriverArgs pi 产出（无 oc 附加项、无 standalone @）；findLatestSessionId pi 守卫。
  - `agents/build.pi.md`（新）— mission-driver 通用执行 persona（遵循 AGE、做 step 工作、输出恰好一个 `<AI_STEP_RESULT>`），经 `--append-system-prompt @{agentFile}` 加载。
  - owner docs: `docs/architecture/mission-driver-baseline.md`（line 13 + Public CLI Surface + 新 §Driver selection）、`tools/mission-driver/README.md`（新 §Driver selection 配置表）、`CONTEXT.md`（配置段 + 关键约束注 pi）、`docs/user-manual.{zh,en}.md`（§2 前各加"切换 pi"小节）。
- Decision（`--append-system-prompt` vs `--system-prompt`）: 选 append（保留 pi 编码默认 prompt + 叠加 persona）。残风险（AGENTS.md 与 persona 指令冲突）经 **real-run smoke 硬 gate** 验证解除——真跑 `demo --step CHECK --driver pi --model zai-coding-cn/glm-5.2`：cmd 行确认 `pi -p ... --append-system-prompt @<abs>/agents/build.pi.md --tools ...`（无 `--pure`/`--variant`/`--dangerously-skip-permissions`），日志含真实 pi 推理产出 + `<AI_STEP_RESULT>pass</AI_STEP_RESULT>` 被引擎解析为 `marker: pass` → `single_step_done`。persona 无指令冲突，无需切 replace。
- Safety scope: 仅 driver 抽象层。未触 engine.js / 零依赖不变式（仅用 Node 内建 `node:url`）/ web/dist / memory/_index.md / install-age.sh / flow JSON / exitMap / mission.json schema（`agentFile` 是计算字段非 schema字段）。`--driver` 属 CLI surface 附加式（opencode 默认零变化），owner doc（架构 baseline）已同步描述新契约，满足 ai-autonomy-policy。
- Verification（scoped）: `node --test test/pi-driver-config.test.js` → **10 pass / 0 fail**。全量 `pnpm --prefix tools/mission-driver test` → 607 tests / 604 pass / 3 fail；3 fail 均为**预存/flaky**（已 git stash baseline 验证：mission-check CLI Case D pathToFileURL 平台相关 + FlowEngine null marker 在 tag-recovery-reconcile；monitor draft 单跑 84/84，full-suite 失败是并发 flaky）—— my changes **零回归**（baseline 595 → with-changes 604，增量恰为新增 10 测试）。real-run smoke 硬 gate 通过（见上）。`run --help` 含 `--driver`；`--driver pi` dry-run 退出 0；不带 `--driver` opencode 行为字节一致。
- Draft review: 两轮独立 subagent（fresh context）—— iter1 `needs revision`（3 minor + 5 note：M1 promptMode 三返回点回归 + runner.js `||"stdin"` 兜底风险 / M2 driver 死代码 / M3 real-run 升硬 gate / N1-N5 reaper+findLatestSessionId+@边界+伪代码）→ 逐条修订 → iter2 `passes draft review`。
- Deferred（已 adjudicated）: pi session 连续性（每 step fresh pi，靠磁盘恢复，触发条件=质量回退或 token 不可接受）；per-step persona 路由（MVP 单 persona 对齐 opencode `--agent build`）；pi 孤儿收割 reaper 失效（tag 在 stdin 不在 cmdline，触发条件=24/7 长跑孤儿堆积）；EXECUTE 步 persona 冲突覆盖（CHECK 已验证，触发条件=审计闭环要分角色）。
